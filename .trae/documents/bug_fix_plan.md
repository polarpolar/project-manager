# 第一阶段第四部分 - Bug 修复实施计划

## [ ] 任务 1：修复二进制文件复制损坏问题（高优先级）
- **优先级**：P0
- **依赖**：无
- **描述**：
  - 修改 `organizeFilesForProjectStart()` 函数，将 `.text()` 读取方式改为 `.arrayBuffer()`
  - 确保二进制文件（PDF、DOCX、图片等）在复制时不会损坏
- **成功标准**：
  - 二进制文件复制后能正常打开
  - 文本文件复制后内容保持不变
- **测试要求**：
  - `programmatic` TR-1.1: 复制 PDF、DOCX、图片和文本文件，验证所有文件都能正常打开
  - `programmatic` TR-1.2: 验证文件大小在复制前后保持一致
- **注意**：确保所有文件读取操作都使用正确的方法

## [ ] 任务 2：修复 addCollectRow() 误触发问题（高优先级）
- **优先级**：P0
- **依赖**：无
- **描述**：
  - 为 `addCollectRow()` 函数添加 `isNew` 参数
  - 只有当 `isNew` 为 true 且任务未完成时，才触发 Toast 和日志
  - 更新所有调用 `addCollectRow()` 的地方，正确传递 `isNew` 参数
- **成功标准**：
  - 编辑已有项目时不会触发 Toast 和日志
  - 用户主动新增催款任务时会触发 Toast 和日志
- **测试要求**：
  - `programmatic` TR-2.1: 打开已有项目，验证不会弹出 Toast
  - `programmatic` TR-2.2: 手动添加催款任务，验证会弹出 Toast
- **注意**：确保所有调用点都正确传递参数

## [ ] 任务 3：修复 XSS 漏洞（高优先级）
- **优先级**：P0
- **依赖**：无
- **描述**：
  - 封装一个统一的 `esc()` 转义函数
  - 在所有 `cardHTML()` 和 `innerHTML` 拼接中使用 `esc()` 函数
  - 确保用户输入在插入到 DOM 前被正确转义
- **成功标准**：
  - 输入 `<script>alert(1)</script>` 不会执行
  - 特殊字符（&, <, >, ") 被正确转义
- **测试要求**：
  - `programmatic` TR-3.1: 创建项目名称包含 `<script>alert(1)</script>`，验证不会弹出警报
  - `programmatic` TR-3.2: 验证特殊字符在显示时正确转义
- **注意**：检查所有用户输入插入点

## [ ] 任务 4：集中定义魔法数字和魔法字符串（中优先级）
- **优先级**：P1
- **依赖**：无
- **描述**：
  - 确保 `STAGE`、`STORAGE_KEY`、`MAX_STORAGE_BYTES` 等常量在文件顶部集中定义
  - 替换代码中所有散落的魔法数字和魔法字符串
- **成功标准**：
  - 代码中不再出现裸写的魔法数字和魔法字符串
  - 所有常量都通过集中定义的变量引用
- **测试要求**：
  - `programmatic` TR-4.1: 搜索代码中不再出现 `stage === 1`、`'pm_projects_v3'` 等裸写常量
  - `programmatic` TR-4.2: 验证所有常量引用正确
- **注意**：确保替换所有出现的地方，包括注释中的引用